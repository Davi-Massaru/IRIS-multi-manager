import {Component,input,signal,OnInit,OnChanges,SimpleChanges} from '@angular/core';
import {JsonPipe} from '@angular/common';
import {api,FleetResult} from '../api';
@Component({selector:'events-workspace',imports:[JsonPipe],template:`<button class="primary" (click)="load()" [disabled]="busy()">Refresh audit events</button>@if(error()){<p class="error">{{error()}}</p>}@if(result();as result){@for(target of result.results;track target.instanceId){<section><h3>{{target.instanceName}} · {{target.status}}</h3><pre>{{target.data|json}}</pre></section>}}`})
export class EventsWorkspace implements OnInit,OnChanges{selected=input.required<string[]>();busy=signal(false);error=signal('');result=signal<FleetResult|null>(null);ngOnInit(){void this.load();}ngOnChanges(c:SimpleChanges){if(!c['selected'].firstChange)void this.load();}async load(){if(!this.selected().length)return;this.busy.set(true);try{this.result.set(await api<FleetResult>(`/events?instances=${this.selected().join(',')}&maxRows=100`));}catch(e){this.error.set(e instanceof Error?e.message:'Events request failed');}finally{this.busy.set(false);}}}
